<%= @user.name || @user.email %>, you are welcome!

<%= button_to "send email", email_path(@user) %>


<p><strong><%= @user.name || @user.email %>'s topics </strong></p>

<%= render partial: "topics/form", locals: {topic: Topic.new, user: @user} %>

<h3><p> Topics and Bookmarks you have Created </p></h3>

<div class="row">
<div class="col-xs-12">
  <%= render partial: "topics/topic", collection: @topics %>
  </div>
</div>
<div class="row">
  <div class="col-xs-12">
    <div class="caption">
      <h3><p> Bookmarks you have Upvoted </p></h3>
    </div> 
    <div class="row">
      <%= render partial: "bookmarks/bookmark", collection: @upvoted_bookmarks, as: :upvoted_bookmark %>
      <br> 
      </div>

      <%= will_paginate @upvoted_bookmarks, renderer: BootstrapPagination::Rails%>
  </div>
</div> 